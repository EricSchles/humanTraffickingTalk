from subprocess import *

# domain = str(raw_input("please specify the domain you want to scrape"))
# subpage1 = str(raw_input("Please specify the first starting page"))
# subpage2 = str(raw_input("Please specify the second starting page"))
# subpage3 = str(raw_input("Please specify the third starting page"))
# subpage4 = str(raw_input("Please specify the fourth starting page"))

# call(['python', 'config.py', domain, subpage1, subpage2, subpage3, subpage4])
#generate the spider
call(['python', 'generate_craigslist.py'])
#run the spider
call(['python', 'link_scrap.py'])
